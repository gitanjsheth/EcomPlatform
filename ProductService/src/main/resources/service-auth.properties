# Service-to-Service Authentication Configuration
# For development only - in production, use environment variables

# Internal service token for development
internal.service.token=internal-service-secret-2024

# Authorized services
authorized.services=CartService,OrderManagementService,PaymentService

# Allow localhost for development
allow.localhost=true